namespace ExtSort.Code.Constants
{
    internal class VerbDescriptions
    {
        public const string GeneratorDesc = "Generates a file which contains lines in the <number>. <string> format";
        public const string SorterDesc = "Sorts a file with the data represented in the <number>. <string> pattern ordered by <string> then by <number>";
        public const string EvaluateDesc = "Generates an appsettings.json file with optimal settings considering the current environment";
    }
}
